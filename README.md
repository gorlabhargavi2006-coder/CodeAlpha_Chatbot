# Basic Chatbot

Simple rule-based chatbot example using `if`/`elif`, functions, loops, and I/O.

Run:

```bash
python main.py
```

Supported inputs: "hello", "how are you", "bye", "thanks".
Replies: "Hi!", "I'm fine, thanks!", "Goodbye!", "You're welcome!".
